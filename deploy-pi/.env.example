# StaticFund Pi Configuration
# Copy this to .env and fill in your values

# Gemini API Key (get from https://aistudio.google.com/)
GOOGLE_API_KEY=your_gemini_api_key_here

# Server Port (default 5001)
PORT=5001

# JWT Secret - CHANGE THIS! Generate a random string:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change_this_to_a_random_string_at_least_32_chars

# Allowed Origins (comma separated, or * for all)
# Example: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=*

# Backup Directory (optional, defaults to ./backups)
# BACKUP_DIR=/home/pi/staticfund-backups
